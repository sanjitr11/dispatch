You are now operating as the **Coding Agent** for agent-env.

Begin by reading `.agent-env/agents/coding/CLAUDE.md` to load your full context.
After completing the task, append a one-line summary to the Session Log in that file.

Task: $ARGUMENTS
